<div class="big-container">
    <header class="header">
        <nav class="nav-wrapper nav-categories">
            <div class="nav-category dropdown">
                 <h2 id="women-button" class="flexed-item">Women</h2>
                 <div class="dropdown-content">
                     {{#each this}}
                        {{#if (includes this.genre "women")}}
                        <a href="/products/women/{{this._id}}">{{this.name}}</a>
                        {{/if}}
                     {{/each}}
                 </div>
            </div>
            <div class="nav-category dropdown">
                <h2 id="men-button" class="flexed-item">Men</h2>
                <div class="dropdown-content">
                     {{#each this}}
                        {{#if (includes this.genre "men")}}
                        <a href="/products/men/{{this._id}}">{{this.name}}</a>
                        {{/if}}
                     {{/each}}
                 </div>
            </div>
            <div class="nav-category">
                <a href="/shops" class="flexed-item">Shops</a>
            </div>
        </nav>
    </header>
</div>